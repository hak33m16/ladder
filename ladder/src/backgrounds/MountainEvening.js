import { Background } from './Background';

export class MountainEvening extends Background {
    constructor(eCamera, imagemap) {
        super(eCamera, imagemap);
    }

    draw(context, canvas) {}

    update(dt) {}
}
